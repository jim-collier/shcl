# A quote inside a bare value is content, not the start of a quoted piece.
note: don't panic  # keep this
b: it's fine, ok
c: say "hi" there, x
d: "quoted, comma", 'single'
list:
	* it's an element  # c
	* "quoted # not a comment"
e: name's # c
url: "http://h/#frag"
f: "a:b" # c
