# base: two servers and a couple of leaves
retries: 3
server: web1
	port: 8080
	host: a.example
server: web2
	port: 9090
	# just a note
mode:
# over layer: wrapper mentions must merge, not wipe
server:
