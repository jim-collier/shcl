# A block given twice loads as one. An edit that rewrites the line above
# the second header would leave that header out, so the save gives the
# canonical form.
db:
	host: localhost
		pool: 4
	port: 5432
